# Testing Rules

- Prefer fixture-backed unit tests with fake requesters.
- Test adapters, normalization, and MCP registration separately.
- Use live smoke tests only when explicitly enabled.
- Verify fail-open warnings for partial upstream failures.
